<div id="header">
    <div class="container">

        <!-- Logo -->
        <div id="logo">
            <h1><a href="@Url.Action("Index", "Home")">@Strings.Logo</a></h1>
        </div>

        <!-- Nav -->
        <nav id="nav">
            <ul>
                @Html.MenuLink(Strings.MainPage, "Index", "Home")
                @Html.MenuLink(Strings.News, "ListAdmin", "News")
                @Html.MenuLink(Strings.Services, "ListAdmin", "Services")
                @Html.MenuLink(Strings.Gallery, "Index", "Gallery")
                @Html.MenuLink(Strings.About, "About", "Home")
            </ul>
        </nav>
    </div>
</div>

<div id="banner">
    <div class="container">
    </div>
</div>