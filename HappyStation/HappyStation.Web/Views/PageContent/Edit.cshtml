@model PageContentViewModel

@{
    ViewBag.Title = Strings.Edit;
    Layout = "/Views/Shared/_AdminLayout.cshtml";
}

<div id="main" class="container">
    <div class="9ut">
        <h2>@ViewBag.Title</h2>

        @using (Html.BeginForm("Save", "PageContent", FormMethod.Post, new { enctype = "multipart/form-data" }))
        {
            @Html.AntiForgeryToken()
            @Html.ValidationSummary()

            @Html.HiddenFor(m => m.Id)

            <fieldset>
                <ol>
                    <li>
                        @Html.LabelFor(m => m.Type)
                        @Html.EnumDropDownListFor(model => model.Type)
                    </li>
                    <li>
                        @Html.LabelFor(m => m.Text)
                        @Html.EditorFor(m => m.Text)
                    </li>
                </ol>
                <input type="submit" value="@Strings.Save" />
            </fieldset>
        }
    </div>
</div>

